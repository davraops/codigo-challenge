name: App Worker PR

on:
  pull_request:
    branches:
      - main
    paths:
      - 'app/worker/**'

env:
  GO_VERSION: ${{ vars.GO_VERSION || '1.22' }}
  POSTGRES_HOST: ${{ vars.POSTGRES_HOST || 'localhost' }}
  POSTGRES_PORT: ${{ vars.POSTGRES_PORT || '5432' }}
  POSTGRES_USER: ${{ vars.POSTGRES_USER || 'test' }}
  POSTGRES_DB: ${{ vars.POSTGRES_DB || 'test' }}
  NATS_URL: ${{ vars.NATS_URL || 'nats://localhost:4222' }}
  WORKER_STARTUP_WAIT_SECONDS: ${{ vars.WORKER_STARTUP_WAIT_SECONDS || '5' }}
  DOCKER_IMAGE_NAME: ${{ vars.DOCKER_IMAGE_NAME_WORKER || 'codigo-worker' }}
  CONTAINER_NAME: ${{ vars.CONTAINER_NAME_WORKER || 'worker-test' }}
  GOLANGCI_LINT_TIMEOUT: ${{ vars.GOLANGCI_LINT_TIMEOUT || '5m' }}

jobs:
  title-check:
    name: Title Check
    runs-on: dedicated-runner
    steps:
      - name: Validate PR title
        uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            style
            refactor
            test
            chore
            ci
            build
            perf
            revert
          requireScope: false
          subjectPattern: ^.{1,100}$
          subjectPatternError: |
            The subject must be between 1 and 100 characters long.

  format-check:
    name: Format Check
    runs-on: dedicated-runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            app/worker

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run gofmt
        working-directory: app/worker
        run: |
          if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then
            echo "Code is not formatted. Run 'go fmt ./...' to fix."
            gofmt -d .
            exit 1
          fi

  lint:
    name: Lint
    runs-on: dedicated-runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            app/worker

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run go vet
        working-directory: app/worker
        run: go vet ./...

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest
          working-directory: app/worker
          args: --timeout=${{ env.GOLANGCI_LINT_TIMEOUT }}

  test:
    name: Unit Tests
    runs-on: dedicated-runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            app/worker

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Download dependencies
        working-directory: app/worker
        run: go mod download

      - name: Run tests
        working-directory: app/worker
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: always
        with:
          file: ./app/worker/coverage.out
          flags: worker
          name: worker-coverage

  typecheck:
    name: Type Check
    runs-on: dedicated-runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            app/worker

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Download dependencies
        working-directory: app/worker
        run: go mod download

      - name: Build (type check)
        working-directory: app/worker
        run: go build -o /dev/null ./...

  docker-build:
    name: Docker Build
    runs-on: dedicated-runner-dind
    needs: [title-check, format-check, lint, test, typecheck]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            app/worker

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        working-directory: app/worker
        run: |
          docker build -t ${{ env.DOCKER_IMAGE_NAME }}:pr-${{ github.event.pull_request.number }} .

  healthcheck:
    name: Health Check
    runs-on: dedicated-runner-dind
    needs: [docker-build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            app/worker

      - name: Build Docker image
        working-directory: app/worker
        run: |
          docker build -t ${{ env.DOCKER_IMAGE_NAME }}:pr-${{ github.event.pull_request.number }} .

      - name: Run container
        run: |
          docker run -d --name ${{ env.CONTAINER_NAME }} \
            -e POSTGRES_HOST=${{ env.POSTGRES_HOST }} \
            -e POSTGRES_PORT=${{ env.POSTGRES_PORT }} \
            -e POSTGRES_USER=${{ env.POSTGRES_USER }} \
            -e POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }} \
            -e POSTGRES_DB=${{ env.POSTGRES_DB }} \
            -e NATS_URL=${{ env.NATS_URL }} \
            ${{ env.DOCKER_IMAGE_NAME }}:pr-${{ github.event.pull_request.number }}

      - name: Wait and verify container is running
        run: |
          echo "Waiting for container to start..."
          sleep ${{ env.WORKER_STARTUP_WAIT_SECONDS }}
          
          # Check if container is still running (not crashed)
          if ! docker ps | grep -q ${{ env.CONTAINER_NAME }}; then
            echo "❌ Container stopped unexpectedly!"
            docker logs ${{ env.CONTAINER_NAME }}
            exit 1
          fi
          
          # Check if process is running inside container
          if docker exec ${{ env.CONTAINER_NAME }} ps aux | grep -q "[w]orker"; then
            echo "✅ Container is running and process is active!"
          else
            echo "❌ Worker process not found in container!"
            docker logs ${{ env.CONTAINER_NAME }}
            exit 1
          fi

      - name: Cleanup
        if: always()
        run: |
          docker stop ${{ env.CONTAINER_NAME }} || true
          docker rm ${{ env.CONTAINER_NAME }} || true

  comment-pr:
    name: Comment PR with Findings
    runs-on: dedicated-runner
    needs: [title-check, format-check, lint, test, typecheck, docker-build, healthcheck]
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Comment PR
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 📋 App Worker PR Checks Summary

            | Check | Status |
            |-------|--------|
            | **Title Check** | ${{ needs.title-check.result == 'success' && '✅ Passed' || '❌ Failed' }} |
            | **Format Check** | ${{ needs.format-check.result == 'success' && '✅ Passed' || '❌ Failed' }} |
            | **Lint** | ${{ needs.lint.result == 'success' && '✅ Passed' || '❌ Failed' }} |
            | **Unit Tests** | ${{ needs.test.result == 'success' && '✅ Passed' || '❌ Failed' }} |
            | **Type Check** | ${{ needs.typecheck.result == 'success' && '✅ Passed' || '❌ Failed' }} |
            | **Docker Build** | ${{ needs.docker-build.result == 'success' && '✅ Passed' || '❌ Failed' }} |
            | **Health Check** | ${{ needs.healthcheck.result == 'success' && '✅ Passed' || '❌ Failed' }} |

            ### Details
            - **Title Check**: Validates PR title follows Conventional Commits format (type: description)
            - **Format Check**: Verifies code formatting with `gofmt`
            - **Lint**: Runs `go vet` and `golangci-lint` for code quality
            - **Unit Tests**: Executes tests with race detector and coverage
            - **Type Check**: Validates type correctness with `go build`
            - **Docker Build**: Ensures Docker image builds successfully
            - **Health Check**: Verifies the container starts and process is running

            ${{ needs.title-check.result != 'success' && '⚠️ **Title Check failed** - PR title must follow format: `type: description` (e.g., `feat: add new endpoint`, `fix: resolve bug`). Valid types: feat, fix, docs, style, refactor, test, chore, ci, build, perf, revert.' || '' }}
            ${{ needs.format-check.result != 'success' && '⚠️ **Format Check failed** - Run `go fmt ./...` to fix formatting issues.' || '' }}
            ${{ needs.lint.result != 'success' && '⚠️ **Lint failed** - Review linting errors above.' || '' }}
            ${{ needs.test.result != 'success' && '⚠️ **Tests failed** - Review test failures above.' || '' }}
            ${{ needs.typecheck.result != 'success' && '⚠️ **Type Check failed** - Fix type errors to proceed.' || '' }}
            ${{ needs.docker-build.result != 'success' && '⚠️ **Docker Build failed** - Fix Dockerfile or build issues.' || '' }}
            ${{ needs.healthcheck.result != 'success' && '⚠️ **Health Check failed** - Container failed to start or process is not running.' || '' }}

            ${{ needs.title-check.result == 'success' && needs.format-check.result == 'success' && needs.lint.result == 'success' && needs.test.result == 'success' && needs.typecheck.result == 'success' && needs.docker-build.result == 'success' && needs.healthcheck.result == 'success' && '🎉 All checks passed! Ready for review.' || '' }}

